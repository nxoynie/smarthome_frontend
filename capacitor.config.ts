import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'smarthome_frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
